import { Request, Response } from "express";
import { Thought } from "../models";


export const getAllThoughts = async(_req: Request, res: Response) => {
    try {
        const users = await Thought
    }
}